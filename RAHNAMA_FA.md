# راهنمای کاربری — مرکز کنترل عملیات TotalRÊNOTECH

نرم‌افزار عملیاتی روی همان فایل Google Sheet فعلی ساخته شده است. شیت همچنان
منبع اصلی داده است؛ این برنامه فقط یک رابط سریع و حرفه‌ای روی آن است.

---

## ۱. ورود

با حساب Google شرکت وارد شوید. فقط ایمیل‌هایی که در فهرست مجاز ثبت شده‌اند
اجازهٔ ورود دارند. چهار سطح دسترسی وجود دارد:

| نقش | دسترسی |
|---|---|
| مدیر (admin) | همه چیز، شامل بازگردانی تغییرات و تنظیمات |
| هماهنگ‌کننده (coordinator) | خواندن، ثبت، تکمیل، گزارش تغییرات، صندوق هوش مصنوعی |
| تکنسین (technician) | دیدن کارها و ثبت گزارش روزانه |
| فقط‌خواندنی (readonly) | فقط مشاهده |

---

## ۲. داشبورد

۲۳ کارت شاخص زنده: کل کارها، کارهای فعال، پیش‌رو، فردا، در حال اجرا،
تکمیل امروز/هفته/ماه، عقب‌افتاده، منتظر مصالح، منتظر تأیید، فوری، یادآوری‌های
باز و عقب‌افتاده، ساعت کار امروز/هفته/ماه، پروژه‌های بلندمدت، بدون تکنسین،
تکنسین‌های فعال و کامیون‌های در دسترس.

**هر کارت و هر ستون نمودار قابل کلیک است** و مستقیم به فهرست فیلترشدهٔ همان
مورد می‌رود.

سه نمودار: کار بر اساس وضعیت، اولویت و نوع پروژه.

---

## ۳. کارها

یازده نمای آماده: همه، پیش‌رو، برنامهٔ فردا، در حال اجرا، منتظر مصالح،
منتظر تأیید، تکمیل‌شده، لغوشده، پروژه‌های بلندمدت، بدون تکنسین، عقب‌افتاده.

- روی هر ردیف بزنید تا پنجرهٔ جزئیات باز شود.
- تیم را با انتخاب چندتایی مشخص کنید (حداکثر ۵ نفر) — مثل «علی + میثم» یا
  «ارسلان + پیروز + فرزاد».
- آدرس در برنامه به‌صورت یک «آدرس کامل» نمایش داده می‌شود، ولی در شیت همان
  ستون‌های جدا باقی می‌ماند.
- **تغییر وضعیت روی All Jobs ثبت می‌شود** و نماهای Upcoming، Ongoing، Done و
  بقیه خودکار به‌روز می‌شوند. آن شیت‌ها فرمولی هستند و برنامه هرگز مستقیم روی
  آن‌ها نمی‌نویسد.

---

## ۴. تکمیل‌شده

چهار بازه: امروز، این هفته، این ماه، و آرشیو کامل.
جمع‌ها: تعداد پروژه، تعداد تکنسین، ساعت کار، نفر-ساعت، و تعداد دارای مصالح.

با شروع ماه جدید، بازهٔ ماه خودکار جلو می‌رود. **هیچ رکوردی حذف نمی‌شود** —
سوابق قدیمی همیشه در آرشیو در دسترس است.

---

## ۵. یادآوری‌ها

فقط دو بخش: **ACTIVE REMINDERS** و **ARCHIVE**.

قواعد خودکار:

- یک ردیف برای هر یادآوری.
- مشتری یکسان + اقدام یکسان = همان یادآوری، ادغام می‌شود.
- وضعیت COMPLETED، CANCELLED یا REMOVED → خودکار به ARCHIVE منتقل می‌شود.
- «COMPLETED — CHECK REQUIRED» تا بررسی انسانی در ACTIVE می‌ماند.
- موارد عقب‌افتاده اول فهرست می‌آیند.
- مبالغ به دلار کانادا نمایش داده می‌شوند.

---

## ۶. مصالح

هر کاری که در ستون Materials چیزی داشته باشد خودکار اینجا دیده می‌شود و به
همان کار وصل می‌ماند. دکمهٔ «Needs purchasing only» فقط مواردی را نشان می‌دهد
که وضعیت‌شان NEED LIST، NEED PURCHASE یا ORDERED است.

---

## ۷. گزارش روزانه

برای پروژه‌های چندروزه، هر روز کاری یک ردیف:

**ساعت هر نفر = (ساعت خروج − ساعت ورود) − استراحت**
**نفر-ساعت = تعداد نفرات × ساعت هر نفر**

محاسبه قبل از ذخیره در پایین پنجره نشان داده می‌شود.
در پایان پروژه، جمع‌بندی خودکار: تعداد روز کاری، تعداد نفرات، مجموع نفر-ساعت،
میانگین تیم، تاریخ شروع و آخرین روز.

---

## ۸. تیم و ناوگان

پروندهٔ دائمی پرسنل و خودروها. همین فهرست منبع همهٔ منوهای انتخاب تیم و
کامیون در کل برنامه است. سلول خالی یعنی اطلاعات هنوز تأیید نشده — **هیچ
اطلاعاتی ساخته نشده است**.

---

## ۹. صندوق هوش مصنوعی

پیام را به **فارسی، فینگلیش، انگلیسی یا فرانسه** بنویسید. مثال:

> «Winners #451 در 225 Rue Peel پیش‌رو است. فردا میثم و پیروز با کامیون ۷ بروند.»

روی «بازبینی» بزنید. برنامه:

1. اطلاعات را استخراج می‌کند و **خلاصهٔ دقیق** نشان می‌دهد.
2. اگر رکورد مشابهی وجود داشته باشد اعلام می‌کند تا **رکورد تکراری ساخته نشود**.
3. فیلدهای نامعلوم را خالی می‌گذارد — **هرگز حدس نمی‌زند**.
4. درصد اطمینان را نشان می‌دهد.

تا وقتی «ثبت در شیت» را نزنید **هیچ چیزی نوشته نمی‌شود**. بعد از ثبت، شمارهٔ
دقیق شیت و ردیف را برمی‌گرداند.

---

## ۱۰. گزارش تغییرات

هر تغییر — از برنامه، از ChatGPT یا از Claude — با زمان، کاربر، منبع، مقدار
قبلی و مقدار جدید ثبت می‌شود. تغییرات تک‌فیلدی را می‌توان با دکمهٔ Undo
برگرداند.

---

## ۱۱. موبایل

برنامه روی اندروید و آیفون کار می‌کند و قابل نصب است
(Add to Home Screen). نوار پایین: داشبورد، کارها، یادآوری‌ها، مصالح،
صندوق هوش مصنوعی.

دکمهٔ زبان در نوار بالا برنامه را کامل به فارسی و چیدمان راست‌به‌چپ می‌برد.

---

## ۱۲. نکات ایمنی

- هیچ کلید یا رمزی در مرورگر قرار نمی‌گیرد؛ همهٔ دسترسی‌ها سمت سرور است.
- برنامه هرگز روی شیت‌های فرمولی (Upcoming، Ongoing، Done، …) نمی‌نویسد.
- نوشتن فقط روی سلول‌های مشخص انجام می‌شود، نه کل ردیف.
- درخواست تکراری از ChatGPT یا Claude رکورد دوم نمی‌سازد.
